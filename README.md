# Chatbottle

An opinionated local chat app. Uses the bottle framework, with the entire framework included as a file.

Great for keeping an anonymous chat system in a LAN to share quick URLs and snippets across devices.

URLs are highlighted and converted into links.

## Usage

`python3 chatbottle.py`

